# SIVABALAJI-R-OOPS-IA-1


#Problem Statement
    Develop a Java program to demonstrate      inheritance and method overriding using an insurance claims scenario. Create a superclass Claim with claimId and amount attributes and a constructor to initialize them. Create a subclass MedicalClaim that adds the hospitalName attribute. Override the display() method in MedicalClaim to display all the claim details.